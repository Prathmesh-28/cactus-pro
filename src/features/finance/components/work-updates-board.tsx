// stub — work-updates-board uses react-query internally
export function WorkUpdatesBoard() {
  return (
    <div className="rounded-lg border border-[var(--border)] bg-[var(--card)] p-8 text-center">
      <p className="text-sm text-[var(--muted-foreground)]">Work updates board — coming soon.</p>
    </div>
  );
}
