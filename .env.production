# Production build config (VITE_ vars are public — baked into the client bundle).
# Used automatically by `npm run build`. The mobile APK talks to this backend.
VITE_API_URL=https://cactus-pro-backend.onrender.com
